# Runbook: боевая установка

Что развёрнуто, где лежит, что проверено и какие грабли уже найдены.
Дополняет [README.md](README.md) (как устроен проект) и [DEPLOY.md](DEPLOY.md)
(как развернуть с нуля).

Дата ввода в эксплуатацию: **30 августа 2026.**

---

## 1. Что где

| | |
|---|---|
| Сервер | `201.34.132.171`, Ubuntu 26.04 LTS, 2 vCPU, 3.8 ГБ RAM, 48 ГБ диск |
| Каталог проекта | `/opt/incident-bot` |
| Домен | `metricsi.ru` (A-запись уже указывала на этот сервер) |
| Webhook | `https://metricsi.ru/webhook/max` |
| Бот в MAX | «Искра», `@id400902045875_3_bot`, id `391053284` |
| Node на хосте | v22 — нужен для тестов, миграций, seed и `npm run doctor` |

Три контейнера, все с `restart: unless-stopped`, docker в автозапуске —
установка переживает перезагрузку сервера:

```
incident-bot-app-1        приложение
incident-bot-caddy-1      TLS-терминация (Let's Encrypt, автопродление)
incident-bot-postgres-1   PostgreSQL 16
```

Порт `3000` приложения слушает **только loopback** — единственный вход снаружи
идёт через Caddy по HTTPS. Кроме `/webhook/*` и `/health` домен отдаёт 404 и не
рассказывает, что за ним стоит.

### Рабочие чаты

| Назначение | Chat ID |
|---|---|
| Распределение | `-78339479576634` |
| Согласование | `-78339493797946` |
| Профильные группы | 50 чатов: 1 региональный, 26 муниципальных, 23 исполнительных; см. `/groups` |

Темы пользователя и профильные группы разделены. Тема остаётся в обращении и
отчёте, но не выбирает чат. При распределении бот показывает три направления:
«Калужская область», «Органы местного самоуправления» и «Органы исполнительной
власти». Одно обращение можно отправить только в одну группу.

#### Региональная и муниципальные группы

| Код | Группа | Chat ID |
|---|---|---:|
| `REGION_KALUGA` | Калужская область | `-78347547385914` |
| `LG_BABYNINSKY` | Бабынинский район | `-78339541704762` |
| `LG_BARYATINSKY` | Барятинский район | `-78347519270970` |
| `LG_BOROVSKY` | Боровский район | `-78347583430714` |
| `LG_KALUGA_CITY` | Город Калуга | `-78344795004986` |
| `LG_OBNINSK_CITY` | Город Обнинск | `-78344672059450` |
| `LG_DZERZHINSKY` | Дзержинский район | `-78347616526394` |
| `LG_DUMINICHSKY` | Думиничский район | `-78347487354938` |
| `LG_ZHIZDRINSKY` | Жиздринский район | `-78347446067258` |
| `LG_ZHUKOVSKY` | Жуковский район | `-78347408711738` |
| `LG_IZNOSKOVSKY` | Износковский район | `-78347367489594` |
| `LG_KIROVSKY` | Кировский район | `-78347321548858` |
| `LG_KOZELSKY` | Козельский район | `-78347277377594` |
| `LG_KUYBYSHEVSKY` | Куйбышевский район | `-78347236483130` |
| `LG_LYUDINOVSKY` | Людиновский район | `-78347197816890` |
| `LG_MALOYAROSLAVETSKY` | Малоярославецкий район | `-78347154825274` |
| `LG_MEDYNSKY` | Медынский район | `-78344994365498` |
| `LG_MESHCHOVSKY` | Мещовский район | `-78344957075514` |
| `LG_MOSALSKY` | Мосальский район | `-78344918540346` |
| `LG_PEREMYSHLSKY` | Перемышльский район | `-78344880791610` |
| `LG_SPAS_DEMENSKY` | Спас-Деменский район | `-78344840159290` |
| `LG_SUKHINICHSKY` | Сухиничский район | `-78344714133562` |
| `LG_TARUSSKY` | Тарусский район | `-78341985280058` |
| `LG_ULYANOVSKY` | Ульяновский район | `-78341943140410` |
| `LG_FERZIKOVSKY` | Ферзиковский район | `-78339531415610` |
| `LG_KHVASTOVICHSKY` | Хвастовичский район | `-78540154609722` |
| `LG_YUKHNOVSKY` | Юхновский район | `-78540182331450` |

`REGION_KALUGA` — особая группа: отвечать в ней могут распределители, готовый
ответ отправляется пользователю без чата согласования. Для остальных групп
согласование обязательно.

#### Органы исполнительной власти

| Код | Группа | Chat ID |
|---|---|---:|
| `EA_INTERNAL_POLICY` | Министерство внутренней политики | `-78540262744122` |
| `EA_TRANSPORT` | Министерство транспорта | `-78540295643194` |
| `EA_HEALTH` | Министерство здравоохранения | `-78540333064250` |
| `EA_COMPETITION` | Министерство конкурентной политики | `-78540368191546` |
| `EA_CULTURE_TOURISM` | Министерство культуры и туризма | `-78540395061306` |
| `EA_EDUCATION` | Министерство образования и науки | `-78541146824762` |
| `EA_NATURE` | Министерство природных ресурсов и экологии | `-78541177495610` |
| `EA_AGRICULTURE` | Министерство сельского хозяйства | `-78541205741626` |
| `EA_SPORT` | Министерство спорта | `-78541235036218` |
| `EA_CONSTRUCTION` | Министерство строительства и ЖКХ | `-78541262430266` |
| `EA_LABOR` | Министерство труда и социальной защиты | `-78541293690938` |
| `EA_FINANCE` | Министерство финансов | `-78541323968570` |
| `EA_DIGITAL` | Министерство цифрового развития | `-78541348479034` |
| `EA_ECONOMY` | Министерство экономического развития | `-78541370695738` |
| `EA_GZHI` | ГЖИ | `-78541410148410` |
| `EA_UATK` | УАТК | `-78541435707450` |
| `EA_ZAGS` | ЗАГС | `-78541700735034` |
| `EA_ARCHIVES` | Управление по делам архивов | `-78541740843066` |
| `EA_CULTURAL_HERITAGE` | Управление по охране объектов культурного наследия | `-78541767450682` |
| `EA_ARCHITECTURE` | Управление архитектуры и градостроительства | `-78541797204026` |
| `EA_VETERINARY` | Комитет ветеринарии | `-78541827219514` |
| `EA_YOUTH` | Управление молодежной политики | `-78541851336762` |
| `EA_GOSSTROYNADZOR` | Госстройнадзор | `-78541877878842` |

#### Новый путь обращения

1. При первом обращении пользователь обязательно указывает фамилию и имя
   (отчество — если есть) и номер телефона. Кнопка «Поделиться контактом»
   получает контакт владельца аккаунта через штатный механизм MAX; ручной ввод
   остаётся запасным вариантом. После подтверждения карточки ФИО и телефон
   сохраняются в профиле и автоматически подставляются в следующие обращения.
   Изменение этих полей в карточке обновляет профиль. Это не отменяет политику
   хранения: неактивный профиль без связанных обращений удаляется по правилу
   90 дней, после чего данные и подтверждения документов запрашиваются снова.
2. Выбирает прежнюю тему или «Не знаю».
3. Выбирает территорию из 27 вариантов с перелистыванием. Для округа затем
   выбирает населённый пункт, вводит другой вручную или нажимает «Пропустить».
4. Бот принимает текст и фотографии, но обращение ещё не создаёт. Пользователь
   получает итоговую карточку с ФИО, телефоном, сферой, территорией, текстом и
   приложенными фотографиями (если они есть).
5. «Всё верно» регистрирует обращение и отправляет его в чат «Распределение».
   «Исправить» позволяет отдельно изменить ФИО, телефон, сферу, территорию и
   населённый пункт, текст, заменить или удалить фотографии. После каждого
   изменения бот снова показывает итоговую карточку; остальные поля не
   сбрасываются.
6. Кнопка «Распределить» открывает три направления. В муниципальном списке
   группа, соответствующая территории пользователя, стоит первой с отметкой
   «рекомендуется». Это подсказка, а не автоматическое назначение.
7. Диспетчер выбирает одну группу. Одновременное или повторное назначение во
   второй чат блокируется базой.
8. Обычная группа готовит ответ и отправляет его на согласование. Группа
   «Калужская область» отвечает сразу без согласования.
9. Вместе с итоговым ответом заявитель получает кнопки `1`–`5`, где `1` —
   ответ совсем не помог, а `5` — полностью помог. Принимается только первая
   оценка, только от автора обращения и только после фактической доставки
   ответа. Повторные нажатия не меняют сохранённое значение.

Для «Калужская область (общий вопрос)», «Город Калуга» и «Город Обнинск» бот
сразу переходит к тексту обращения. Для округов он предлагает:

| Территория | Населённые пункты на кнопках |
|---|---|
| Боровский округ | Балабаново, Боровск, Ермолино |
| Дзержинский округ | Кондрово, Товарково, Пятовский, Полотняный Завод, Льва Толстого |
| Жуковский округ | Жуков, Белоусово, Кременки |
| Кировский округ | Киров, Шайковка |
| Людиновский округ | Людиново, Заречный, Букань |
| Малоярославецкий округ | Малоярославец, Детчино |
| Бабынинский округ | Бабынино, Воротынск |
| Барятинский округ | Барятино, Мирный |
| Думиничский округ | Думиничи, Новослободск |
| Жиздринский округ | Жиздра, Совхоз «Коллективизатор» |
| Износковский округ | Износки, Мятлево |
| Козельский округ | Козельск, Сосенский |
| Куйбышевский район | Бетлица, Бутчино |
| Медынский округ | Медынь |
| Мещовский округ | Мещовск, Серпейск |
| Мосальский округ | Мосальск |
| Перемышльский округ | Перемышль |
| Спас-Деменский округ | Спас-Деменск |
| Сухиничский округ | Сухиничи, Середейский |
| Тарусский округ | Таруса, Ильинское |
| Ульяновский округ | Ульяново, Заречье, Дудоровский |
| Ферзиковский округ | Ферзиково, Октябрьский |
| Хвастовичский округ | Хвастовичи, Еленский |
| Юхновский округ | Юхнов, Щелканово |

Под каждым таким списком есть «Другой» и «Пропустить». «Другой» просит
написать название длиной до 100 символов; фотографию на этом шаге бот не
принимает. «Пропустить» сохраняет только выбранный округ.

---

## 2. Что было на сервере до нас

Стояли два чужих прода: `/opt/audience-base` (база рекламных аудиторий) и
`/opt/metricflow` (дашборд соцаналитики на `app.metricsi.ru` / `api.metricsi.ru`).
Оба сняты по решению заказчика.

**Перед удалением снят полный бэкап** — он лежит в двух местах:

* на сервере: `/root/backups/20260830-103934/`
* локально: `Desktop\server-backup-20260830\20260830-103934\`

Внутри: архивы исходников обоих проектов с `.env`, логические дампы обеих баз
(`pg_dump -Fc`), побайтовые копии всех шести docker-томов, манифест,
контрольные суммы и [инструкция по восстановлению](../server-backup-20260830/20260830-103934/RESTORE.md).
Целостность проверена в обеих копиях.

> **Важно:** `/opt/metricflow` **не был git-репозиторием**. `metricflow-src.tar.gz`
> из бэкапа — единственная существующая копия исходников того проекта.
> Не удаляйте её.

---

## 3. Грабли, на которые уже наступили

Здесь то, что стоило времени и наверняка встретится снова.

### 3.1 Сертификаты MAX — самое важное

`*.max.ru` подписан цепочкой **Russian Trusted Sub CA → Russian Trusted Root CA**
(Минцифры). Этого корня нет в стандартном наборе CA, поэтому любой вызов MAX API
падает с `fetch failed`, при том что остальной HTTPS с сервера работает.

Корень лежит в `deploy/russian-trusted-ca.pem` и подключается **только к
процессу бота** через `NODE_EXTRA_CA_CERTS` — в системное хранилище не ставится
и на другие контейнеры не влияет. В `docker-compose.yml` это уже настроено.

**Для любого ручного запуска на хосте переменную надо задать самому:**

```bash
NODE_EXTRA_CA_CERTS=/opt/incident-bot/deploy/russian-trusted-ca.pem npm run doctor
```

`/etc/profile.d/max-ca.sh` существует, но **неинтерактивная SSH-сессия его не
читает** — скрипт, запущенный через `ssh host "команда"`, переменную не увидит и
упадёт на `fetch failed`. Один раз это уже привело к тому, что скрипт привязки
чатов снял две существующие привязки и умер на первой же проверке. Повторный
прогон с переменной всё восстановил, но осадок остался: **всегда указывайте
переменную явно.**

### 3.2 Составные сообщения в MAX

Проверено на живом API, а не по документации:

| Что | Результат |
|---|---|
| текст + фото + кнопки в одном сообщении | **работает** |
| правка сообщения без поля `attachments` | фото и кнопки **сохраняются** |
| правка с передачей `attachments` | список заменяется целиком |

Отсюда три метода в `MaxMessageService`: `send` (всё одним сообщением),
`editCardText` (правит текст, вложения не трогает), `finalizeCard` (гасит
карточку: ни медиа, ни кнопок). Разносить текст и фото по разным сообщениям, как
было сделано изначально, не требуется — второе сообщение появляется только если
в ответе одновременно и фото, и файл (MAX группирует вложения по типу).

### 3.3 tsx не проверяет типы

`npx tsx script.ts` выполнит код с ошибкой типов, а `npm run build` на ней
упадёт. Один раз так и вышло: скрипт отработал, а сборка образа сломалась бы на
следующем деплое. **Перед деплоем всегда `npm run typecheck`** (или `npm run
check`, который делает typecheck + сборку + тесты).

---

## 4. Что проверено

### Автотесты — 127, все зелёные

```bash
cd /opt/incident-bot
TEST_DATABASE_URL='postgresql://incident:incident@localhost:5432/incident_test?schema=public' npm test
```

Интеграционные гоняются на настоящем PostgreSQL и покрывают то, что руками не
воспроизвести: дневной лимит при 6 параллельных запросах, гонку двух
диспетчеров, два одновременных согласования, два одновременных ответа,
неизменность дедлайна через два круга доработок, SLA-уведомления ровно по
одному разу, идемпотентность webhook.

Плюс проверка контракта `@maxhub/max-bot-api`: если библиотека переименует
`handleUpdate`, на котором держится webhook-режим, тест упадёт вместо тихой
остановки бота.

### Живой прогон в MAX

Пройдены сценарии §68 ТЗ: базовый путь, «Не знаю», возврат на доработку с
повторным согласованием, дневной лимит, Excel, блокировка. Подтверждено, что
дедлайн не сдвигается после правок.

---

## 4.1 Нагрузочные замеры

Измерено на этом же сервере (2 vCPU, 3.8 ГБ), а не оценено на глаз.

| Слой | Пропускная способность | Задержка |
|---|---|---|
| Приём webhook (HTTPS → Caddy → Fastify → дедуп) | 460–500 запр/с, 100% успеха при 100 параллельных | медиана 82 мс, p95 275 мс |
| Регистрация обращения (транзакция + счётчик + INSERT) | ~146 обр/с — потолок CPU и БД | при 500 одновременных p95 3.3 с |
| Покой | app 70 МБ, postgres 60 МБ, caddy 15 МБ | из 3.9 ГБ |

Всплеск в 500 одновременных регистраций проходит целиком: запросы встают в
очередь, но не отбиваются.

### Что было исправлено по итогам замеров

Первый прогон 500 одновременных регистраций дал **152 отказа** с
`Unable to start a transaction in the given time`. Причина: Prisma по умолчанию
берёт пул `num_cpus*2+1` — на двух ядрах это 5 соединений, — и даёт всего 2
секунды на ожидание свободного. Сама работа занимает ~60 мс, то есть отказы
были чистой очередью, а не нехваткой ресурсов.

Исправлено двумя настройками:

* `connection_limit=20` в `DATABASE_URL` (см. `DB_CONNECTION_LIMIT` в `.env`);
* явные `maxWait`/`timeout` для транзакций — константа `TRANSACTION_OPTIONS`
  в `src/database/prisma.ts`.

После этого 500 из 500 проходят. Пропускная способность не изменилась (~146/с —
это реальный потолок), но вместо отказа человек просто ждёт. Для бота это
правильный размен: MAX уже получил свой `200 OK`, пользователь ждёт лишь
сообщение с номером.

Все тесты после изменения проходят — гарантия «не более 2 обращений в
сутки» и защита от гонок не пострадали.

### Что упрётся раньше сервера

1. **Люди.** 26 сфер; каждое обращение требует маршрутизации, ответа и
   согласования. Это на порядки меньше 146/с.
2. **Лимиты MAX API.** Не измерялись намеренно — нагружать чужой прод нельзя.
   Если появятся `429`, встроенный retry с экспоненциальной паузой их отработает.
3. **Вложения.** Каждое фото — скачивание из MAX и повторная загрузка. Это
   упирается в канал, а не в CPU, и в замеры выше не входило.

Для ориентира: 146 обр/с — это 525 000 обращений в час. При лимите 2 обращения
на человека в сутки, чтобы это исчерпать, нужно 146 разных людей **каждую
секунду**. Запас относительно любого реалистичного муниципального потока —
трёхзначный.

---

## 4.2 Усиление доступа к PostgreSQL (2 сентября 2026)

Перед изменениями создан и проверен комплект
`backups/pre-hardening-20260902-105312/`: дамп PostgreSQL, архив вложений,
Compose, закрытая копия `.env` и SHA-256. Дамп читается через
`pg_restore --list`; копия данных и вложений также вынесена с сервера.

PostgreSQL больше не опубликован на всех интерфейсах: host-порт `5432`
привязан только к `127.0.0.1`, а приложение продолжает подключаться через
внутреннюю Docker-сеть. Внешняя TCP-проверка подтверждает, что порт закрыт.

Стандартный пароль роли `incident` заменён случайным длинным значением.
`POSTGRES_PASSWORD` и `DATABASE_URL` синхронизированы, секрет хранится только
в серверном `.env` с правами `600`. Старый пароль проверенно отклоняется через
Docker-сеть, новый принимается. После пересоздания PostgreSQL и приложения все
11 обращений остались на месте, `/ready` отвечает, `npm run doctor` завершает
33 проверки без предупреждений и ошибок.

---

## 5. Эксплуатация

### Ежедневное

```bash
cd /opt/incident-bot
docker compose ps                       # состояние
docker compose logs -f app              # логи
NODE_EXTRA_CA_CERTS=./deploy/russian-trusted-ca.pem npm run doctor   # полная проверка
```

Проследить обращение целиком — по номеру, он есть в каждой строке лога:

```bash
docker compose logs app | grep INC-20260830-0001
```

### Обновление кода

```bash
cd /opt/incident-bot
# залить новые src/ tests/ prisma/
npm run typecheck
docker compose --profile tls up -d --build app
```

Миграции применяются автоматически при старте контейнера.

### Полный справочник команд бота

Ниже перечислены **все команды, которые сейчас понимает бот**. Обозначения:

- `<ЗНАЧЕНИЕ>` — обязательный параметр, угловые скобки писать не нужно;
- `[ЗНАЧЕНИЕ]` — необязательный параметр;
- `-` — удалить ранее заданное значение или специальные роли;
- **личный чат** — диалог один на один с ботом;
- **рабочий чат** — групповой чат MAX, куда добавлен бот.

Если бот отвечает «У вас нет прав», сначала проверьте `/whoami`, затем роль и
нужный рабочий чат. Команды администратора технически работают и в личном, и
в групповом чате; для настроек пользователей безопаснее использовать личный
диалог, а для ошибок доставки — закрытый чат «Проблемы».

#### Команды обычного пользователя

| Команда | Где и кому доступна | Что делает |
|---|---|---|
| `/start` | всем, только личный чат | Открывает главное меню создания и просмотра обращений. В группе намеренно ничего не отвечает. |
| `/help` | всем | Показывает доступные команды. Расширенный список сотрудника выводится в рабочем чате при наличии роли. |
| `/rules` | всем, любой чат | Показывает правила подачи обращения. Ничего не изменяет. |
| `/my` | всем, только личный чат | Показывает последние собственные обращения пользователя. Чужие обращения не показывает. |
| `/whoami` | всем, любой чат | Показывает MAX ID пользователя, его роли, ID и тип текущего чата. Нужна перед назначением ролей и привязкой чатов. |

#### Команды сотрудников

| Команда | Кто и где | Что делает и пример |
|---|---|---|
| `/chatid` | `DISPATCHER`, `RESPONDER`, `APPROVER` или `ADMIN`; любой чат | Показывает ID текущего чата. Обычно проще использовать `/whoami`, но команда оставлена для сотрудников. |
| `/incident <НОМЕР>` | любая роль сотрудника; только рабочий чат | Показывает служебную карточку обращения. Пример: `/incident INC-20260902-0011`. |
| `/history <НОМЕР>` | любая роль сотрудника; только рабочий чат | Показывает хронологию обращения: время, смены статуса, исполнителей, версии ответа, возвраты, согласование, SLA и доставку. Пример: `/history INC-20260902-0011`. |
| `/report [ПЕРИОД]` | `DISPATCHER` или `ADMIN`; только рабочий чат | Без параметра показывает кнопки периода. Можно написать `/report today`, `/report 7d`, `/report 30d`, `/report month`, `/report all` или `/report 01.08.2026 - 31.08.2026`. Отправляет Excel-файл в текущий чат. |
| `/resend <НОМЕР>` | любая роль сотрудника; только рабочий чат | Повторно доставляет последний согласованный ответ заявителю. Пример: `/resend INC-20260902-0011`. Если очередь уже имеет код ошибки, используйте выборочный `/delivery_retry <КОД>`. |

Команды `/incident`, `/report` и `/resend` запрещены в личном диалоге даже
администратору, потому что содержат служебные данные и результаты должны
оставаться в рабочем чате.

#### Пользователи, роли и сроки

Эти команды доступны только `ADMIN` и применяются сразу, без перезапуска:

| Команда | Что делает | Пример и важное ограничение |
|---|---|---|
| `/ban <MAX_ID> <ПРИЧИНА>` | Запрещает пользователю создавать новые обращения. | `/ban 7654321 Спам`. Причина обязательна. |
| `/unban <MAX_ID>` | Снимает блокировку пользователя. | `/unban 7654321`. Если блокировки не было, бот так и сообщит. |
| `/role <MAX_ID> <РОЛИ>` | Полностью заменяет специальные роли пользователя. | `/role 7654321 DISPATCHER,APPROVER`. Роли перечислять через запятую без пробелов. Это не добавление к старому списку: неуказанные роли будут сняты. |
| `/role <MAX_ID> -` | Снимает все специальные роли, оставляя обычного заявителя. | Не применяйте к последнему `ADMIN`, пока не проверен второй администратор. |
| `/sla_check` | Немедленно запускает проверку сроков, просрочек и зависших сессий. | Может отправить предусмотренные SLA-предупреждения в рабочие чаты. Обычная автоматическая проверка продолжит работать по расписанию. |
| `/retention_preview` | Без удаления показывает завершённые обращения, которым исполнилось 90 дней, число файлов и их объём. | Работает только в рабочем чате. Всегда выполняйте перед ручным удалением. |
| `/retention_run УДАЛИТЬ` | Удаляет показанные просроченные обращения, ответы, историю, связанные сообщения и локальные файлы обеих сторон. | Требует точного слова `УДАЛИТЬ`. Повторный одновременный запуск блокируется. |
| `/audit` | Показывает 20 последних успешных административных изменений. | Видны время, администратор, объект и понятное описание. Просмотр ничего не изменяет. |

#### Управление темами и ответственными группами

Все команды этого раздела доступны только `ADMIN` и не требуют перезапуска:

| Команда | Что делает | Пример и примечание |
|---|---|---|
| `/categories` | Показывает пользовательские темы: включённые и отключённые. | Темы не содержат chat ID и не управляют маршрутизацией. |
| `/category_add <КОД> <НАЗВАНИЕ>` | Создаёт новую тему. | `/category_add TRANSPORT Общественный транспорт`. |
| `/category_on <КОД>` | Возвращает тему в выбор пользователя. | `/category_on TRANSPORT`. |
| `/category_off <КОД>` | Скрывает тему при создании нового обращения. | Старые обращения и история сохраняются. |
| `/category_name <КОД> <НОВОЕ НАЗВАНИЕ>` | Меняет видимое название темы. | Постоянный код не меняется. |
| `/groups` | Показывает все ответственные группы по трём разделам, их коды, chat ID и состояние. | Для «Калужской области» дополнительно указано «без согласования». |
| `/group_chat <КОД> <CHAT_ID>` | Привязывает или заменяет профильный чат группы. | `/group_chat EA_TRANSPORT -78540295643194`. |
| `/group_on <КОД>` | Включает группу в распределение. | Нужен рабочий chat ID. |
| `/group_off <КОД>` | Скрывает группу из новых назначений. | Старые обращения и история не удаляются. |
| `/group_name <КОД> <НОВОЕ НАЗВАНИЕ>` | Меняет видимое название группы. | Код остаётся прежним. |
| `/group_authority <КОД> <ПОДПИСЬ>` | Задаёт подпись под ответами пользователю. | Значение `-` удаляет подпись. |
| `/group_template <КОД> <ТЕКСТ>` | Задаёт заготовку ответа. | `{{incidentCode}}` заменяется номером; значение `-` удаляет шаблон. |

#### Контроль доставки

Все команды доступны только `ADMIN`:

| Команда | Что делает | Когда применять |
|---|---|---|
| `/delivery_status` | Показывает количества входящих и исходящих записей в ожидании, обработке и состоянии ошибки. | Безопасная первая проверка; ничего не изменяет. |
| `/delivery_errors` | Показывает до 10 последних ошибок: время, обращение, получателя, контекст, причину и короткий код. | Используйте после сообщения в чате «Проблемы». Входящие события помечены как требующие ручной проверки. |
| `/delivery_retry <КОД>` | Возвращает одну выбранную исходящую отправку в очередь и сразу запускает попытку. | Предпочтительный способ. Код копируется из `/delivery_errors`, например `/delivery_retry a1b2c3d4`. |
| `/delivery_retry` | Возвращает в очередь **все** неудачные исходящие отправки. | Используйте только когда понятно, что общая причина устранена. Входящие события эта команда не повторяет. |

Кнопки «Распределить», «Взять в работу», «Согласовать» и другие действия на
карточках не являются отдельными slash-командами. Они описаны в сценариях
работы ниже и защищены от повторных быстрых нажатий.

### История обращений и аудит администраторов

`/history <НОМЕР>` читает уже существующую неизменяемую историю обращения.
Записи идут от создания к последнему событию и показывают, когда менялся
статус, кто выполнил действие, куда распределили обращение, версии ответа,
причины возврата или отклонения, SLA-предупреждения и факт доставки. Команда
доступна всем сотрудникам, но только в рабочем чате.

`/audit` — отдельный журнал успешных команд `ADMIN`. В него записываются:

- блокировка и разблокировка пользователей;
- назначение и снятие ролей;
- создание, включение, отключение и изменение сфер;
- изменение чата, ведомства и шаблона сферы;
- ручной запуск SLA-проверки;
- выборочный или массовый повтор исходящей доставки.

Аудит дополняется только новыми строками: бот не предоставляет команду для
изменения или удаления записей. Тексты обращений туда не копируются; причины
блокировок и понятное описание настройки сохраняются. Токены, пароли, ключи и
строки подключения не записываются. Исторические административные команды,
выполненные до появления таблицы `AdminAuditLog`, задним числом не создаются.

История и аудит развёрнуты 2 сентября 2026 после бэкапа
`daily-20260902-134654`. Миграция `20260902190000_admin_audit` проверена на
отдельной пустой базе; финальный полный прогон — 133/133 теста. На сервере
сохранены 11 обращений и 104 события их истории, `/history` и `/audit`
доступны, новый аудит ожидаемо пуст, `npm run doctor` — 35/35.

### Управление темами и группами — без перезапуска

```
/categories                         список тем пользователя
/category_name ROADS Дороги         переименовать тему
/category_off ROADS                 скрыть тему из выбора
/category_add NEWCODE Название      добавить тему

/groups                             список ответственных групп
/group_chat EA_TRANSPORT <CHAT_ID>  привязать профильный чат
/group_authority EA_TRANSPORT <Подпись>
/group_off EA_TRANSPORT             скрыть группу из распределения
```

Группа без чата диспетчеру **не показывается** — кнопки, которая упадёт после
нажатия, быть не должно.

### Управление ролями — без перезапуска

Роли хранятся в базе данных и применяются сразу. Подключаться к серверу или
перезапускать бот при обычном назначении сотрудников не нужно.

| Роль | Кому назначать | Что разрешает |
|---|---|---|
| `DISPATCHER` | сотруднику распределения | распределять и отклонять обращения, блокировать авторов, делать отчёты; в группе «Калужская область» также готовить окончательный ответ без согласования |
| `RESPONDER` | сотруднику профильного отдела | служебный поиск обращений; работа с карточками своей профильной группы |
| `APPROVER` | сотруднику согласования | принимать ответ или возвращать его на доработку |
| `ADMIN` | только доверенному администратору | все действия и управление ролями, темами, группами и блокировками |

У каждого пользователя также автоматически есть базовая роль `REQUESTER`:
создание и просмотр собственных обращений. Назначать её вручную не нужно.

Кроме роли, добавьте сотрудника в нужный рабочий чат MAX: диспетчера — в чат
распределения, согласующего — в чат согласования, ответственного — в чат его
профильной группы. Для ответственного именно членство в профильном чате даёт
доступ к кнопкам карточек этой группы; роль не открывает чужие профильные чаты.
В чате «Калужская область» кнопки ответа дополнительно требуют роль
`DISPATCHER` или `ADMIN`.

#### Назначить роль

1. Сотрудник пишет боту в личном диалоге команду `/whoami` и передаёт
   администратору свой `MAX ID`.
2. Администратор пишет боту одну из команд:

```
/role 7654321 DISPATCHER
/role 7654321 RESPONDER
/role 7654321 APPROVER
/role 7654321 ADMIN
```

Один человек может совмещать несколько функций. Роли перечисляются через
запятую **без пробелов**:

```
/role 7654321 DISPATCHER,APPROVER
```

Команда каждый раз задаёт пользователю полный список специальных ролей. Если
у человека уже был `DISPATCHER`, команда `/role 7654321 APPROVER` заменит его
на `APPROVER`; для сохранения обеих нужно указать
`DISPATCHER,APPROVER`.

#### Проверить или снять роли

После назначения сотрудник снова отправляет `/whoami`: бот должен показать
новую роль. Общие количества назначенных ролей также выводит `npm run doctor`.

Чтобы снять все специальные права и оставить человека обычным заявителем:

```
/role 7654321 -
```

Изменять роли может только `ADMIN`. Кнопки дополнительно ограничены рабочими
чатами: наличие роли само по себе не позволяет выполнять действие из личного
или постороннего чата.

#### Важная защита от потери доступа

Не снимайте роль у последнего `ADMIN`, пока второй администратор не назначен и
не проверил `/whoami`. Сейчас в базе один `ADMIN`, поэтому снимать с него роль
нельзя.

Если последний администратор всё же удалён, временно укажите его MAX ID в
`ADMINS` серверного `.env`, пересоздайте контейнер `app`, верните постоянную
роль командой `/role <MAX_ID> ADMIN`, затем снова очистите `ADMINS` и
пересоздайте `app`. Списки ролей в `.env` предназначены только для такого
первичного или аварийного доступа: оставленный там ID нельзя лишить прав
обычной командой `/role`.

### Очереди доставки и защита кнопок

Принятые webhook сохраняются в таблице `InboundUpdate` до ответа `200 OK`.
Исходящие сообщения, включая текст, кнопки и временные ссылки на сохранённые
вложения, сначала попадают в `OutboundMessage`. Worker запускается вместе с
приложением, делает немедленную попытку и при сбое повторяет отправку через
30 секунд, 1, 5, 15, 30 и далее 60 минут. После 12 неудач запись получает
`FAILED` и не удаляется автоматически.

Проверка без подключения к серверу — командой администратора в MAX:

```
/delivery_status   # сколько входящих и исходящих ждут или требуют внимания
/delivery_errors   # последние ошибки, получатели, контекст и короткие коды
/delivery_retry a1b2c3d4  # повторить одну выбранную исходящую доставку
/delivery_retry    # вернуть все FAILED исходящие в автоматическую очередь
```

Те же показатели входят в `npm run doctor`; наличие хотя бы одной записи
`FAILED` делает диагностику красной. Входящие `FAILED` нельзя массово
перезапускать вслепую: сначала нужно проверить, успела ли операция изменить
обращение до сбоя.

Команда `/delivery_errors` доступна только `ADMIN`. Она показывает рабочий
контекст, включая данные получателя, если они присутствуют в сообщении, но
обязательно скрывает токены, пароли и ключи. Для выборочного повтора скопируйте
восьмизначный код из списка: `/delivery_retry <КОД>`. Без кода команда, как и
раньше, повторяет все неудачные исходящие сообщения. Входящие ошибки этой
командой не перезапускаются из-за риска повторно выполнить бизнес-действие.

Выборочное управление ошибками развёрнуто 2 сентября 2026 после бэкапа
`daily-20260902-132637`. Полный прогон — 130/130 тестов; после перезапуска
`/delivery_errors` доступна, активных проблем нет, `npm run doctor` — 35/35.

### Чат технических предупреждений

Укажите закрытый технический чат в серверном `.env`:

```env
DELIVERY_ALERT_CHAT_ID=-123456789
```

После пересоздания `app` бот проверяет новые ошибки раз в минуту. Несколько
ошибок объединяются в одно сообщение; полные тексты обращений, MAX ID
пользователей и секреты туда не попадают. Запись получает отметку только после
успешной отправки предупреждения. Поэтому при недоступности MAX бот сообщит о
накопившейся проблеме после восстановления связи. Само предупреждение идёт
мимо обычной outbox-очереди и не может зациклить оповещения.

Проверка доступа бота к этому чату входит в `npm run doctor`. Команда
`npm run doctor -- --send` дополнительно отправляет в чат безвредное
контрольное сообщение.

Функция включена на сервере 2 сентября 2026 после бэкапа
`daily-20260902-130618`. Миграция `20260902163000_delivery_alerts` проверена
на отдельной пустой базе; полный прогон — 128/128 тестов. Контрольное сообщение
из рабочего контейнера доставлено, `npm run doctor` — 35/35.

Каждое нажатие кнопки получает короткую блокировку в `ActionLock`. Повторный
клик отвечает «Уже обрабатывается» и не запускает действие ещё раз. Если
операция длится больше 600 мс, первый клик сразу получает «Принято,
обрабатываю…». При обычной ошибке блокировка снимается, при падении процесса
истекает сама; постоянные изменения статуса дополнительно защищены условными
атомарными обновлениями PostgreSQL.

Успешные записи inbox/outbox хранятся 30 дней. MAX не поддерживает ключ
идемпотентности для `POST /messages`, поэтому авария ровно между приёмом
сообщения MAX и записью `SENT` может дать редкий дубль. Политика намеренно
выбрана в пользу гарантированной доставки, а не молчаливой потери ответа.

Развёрнуто 2 сентября 2026 после проверенного бэкапа
`daily-20260902-124053`. Миграция `20260902150000_reliable_delivery` сначала
прошла на отдельной пустой базе, затем на рабочей. Все 127 тестов прошли на
отдельной PostgreSQL `incident_test`, включая десять одновременных нажатий,
сбой MAX с последующим повтором и дедупликацию входящего webhook. После
развёртывания: 11 обращений на месте, очереди пусты, `npm run doctor` — 34/34,
HTTPS и webhook работают, новых ошибок в журнале нет.

### Хранение и автоматическое удаление обращений

Завершённое обращение хранится 90 календарных дней после окончательного ответа
или отклонения. Активные обращения не удаляются независимо от их возраста.
Ежедневная очистка запускается в `00:00 UTC` (03:00 МСК) плюс случайная задержка
до двух минут — до резервного копирования в 03:30 МСК.

Порядок безопасный: сначала удаляются фотографии и файлы заявителя и ответа,
затем в одной транзакции PostgreSQL удаляются обращение, ответы, история,
сессии, блокировки кнопок и связанные записи доставки. Если хранилище файлов
недоступно, запись остаётся в базе и следующая ежедневная попытка повторяет
удаление. Одновременно может идти только одна очистка.

Установка таймера на сервере:

```bash
install -m 644 deploy/incident-bot-retention.service /etc/systemd/system/
install -m 644 deploy/incident-bot-retention.timer /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now incident-bot-retention.timer
systemctl list-timers incident-bot-retention.timer --all
```

Ручной безопасный просмотр и запуск с сервера:

```bash
cd /opt/incident-bot
docker compose exec -T app npm run retention:preview
docker compose exec -T app npm run retention:run
journalctl -u incident-bot-retention.service -n 100 --no-pager
```

Управление автоматическим запуском:

```bash
# Посмотреть состояние и время следующего запуска
systemctl status incident-bot-retention.timer --no-pager
systemctl list-timers incident-bot-retention.timer --all

# Временно остановить автоматическую очистку
systemctl disable --now incident-bot-retention.timer

# Снова включить автоматическую очистку
systemctl enable --now incident-bot-retention.timer
```

Для внепланового запуска через systemd сначала обязательно выполните
`npm run retention:preview` внутри контейнера и убедитесь, что количество
найденных обращений ожидаемо. Затем:

```bash
systemctl start incident-bot-retention.service
systemctl show incident-bot-retention.service \
  -p Result -p ExecMainCode -p ExecMainStatus
journalctl -u incident-bot-retention.service -n 100 --no-pager
```

Это одноразовая задача, поэтому после завершения состояние сервиса
`inactive (dead)` является нормальным. Успех подтверждают значения
`Result=success` и
`ExecMainStatus=0`, а также отсутствие ошибок в журнале.

Если очистка завершилась с ошибкой:

1. Временно остановите таймер командой
   `systemctl disable --now incident-bot-retention.timer`, чтобы исключить новый
   автоматический запуск во время проверки.
2. Посмотрите причину:
   `journalctl -u incident-bot-retention.service -n 100 --no-pager`.
3. Проверьте контейнеры командой `docker compose ps` из `/opt/incident-bot` и
   устраните указанную в журнале проблему с базой, приложением или хранилищем
   файлов. Не удаляйте записи или файлы вручную.
4. Выполните `docker compose exec -T app npm run retention:preview` и проверьте
   ожидаемое количество объектов.
5. Запустите `systemctl start incident-bot-retention.service`, убедитесь в
   `Result=success`, затем верните расписание командой
   `systemctl enable --now incident-bot-retention.timer`.

То же доступно администратору в закрытом рабочем чате командами
`/retention_preview` и `/retention_run УДАЛИТЬ`. Ручной запуск записывается в
`/audit`. Ошибки автоматического запуска отправляются в технический чат,
заданный через `DELIVERY_ALERT_CHAT_ID`.

Отчёт содержит все ещё хранящиеся поля обращения. Поэтому ежемесячный отчёт
за прошедший месяц нужно формировать в начале следующего месяца; запас в 90
дней позволяет сделать это повторно. После очистки исходный подробный отчёт за
старый период сформировать уже нельзя. В столбце `Оценка ответа (1–5)` хранится
первая оценка заявителя; пустая ячейка означает, что оценка ещё не поставлена.
Событие оценки также видно сотрудникам в `/history <НОМЕР>`.

Важно: удаляются только копии, подконтрольные боту. Сообщения и файлы, уже
доставленные в MAX или сохранённые получателем, локальная очистка удалить не
может. После восстановления старого бэкапа до допуска пользователей выполните
`npm run retention:preview`, затем `npm run retention:run` внутри контейнера.

Резервные копии с данными теперь хранятся не дольше 90 дней: ежедневные — 14,
еженедельные — 70, ежемесячные — 90. Ручные копии с другими именами скрипт не
удаляет; перед production их нужно отдельно проверить и назначить владельца
за удаление.

Функция развёрнута 3 сентября 2026 после проверенного бэкапа
`daily-20260903-130902`. На отдельной базе `incident_test` прошли 138/138
тестов. Первый запуск `incident-bot-retention.service` завершился успешно:
удалено 0 обращений, 0 файлов и 0 профилей, ошибок нет; повторный preview также
показал ноль объектов. Таймер включён, рабочая база сохранила все 14 обращений,
`npm run doctor` — 35/35, очереди доставки пусты.

Это исторический итог первого включения автоочистки. 4 сентября 2026 перед
переходом на новую архитектуру сделан и проверен полный бэкап
`daily-20260904-085004`, после чего 14 тестовых обращений и 3 связанных файла
удалены по решению владельца проекта. Пользователи, роли и настройки сохранены.
На отдельной базе `incident_test` прошли 157/157 тестов. В рабочей базе — 0
обращений и 50 активных профильных групп; итоговая диагностика — 59/59, без
предупреждений и ошибок.

### Резервное копирование

Ежедневный проверяемый бэкап запускает systemd-таймер
`incident-bot-backup.timer`. Расписание — `00:30 UTC` (03:30 МСК) плюс
случайная задержка до 10 минут; `Persistent=true` догоняет пропущенный запуск
после включения сервера.

Скрипт `deploy/backup.sh` атомарно создаёт в `/opt/incident-bot/backups/`:

* PostgreSQL custom dump (`incident.dump`);
* архив локальных вложений (`uploads.tar.gz`);
* закрытые копии `.env` и `docker-compose.yml`;
* метаданные и `SHA256SUMS`.

До публикации каталога скрипт проверяет все SHA-256, читает дамп через
`pg_restore --list` и архив через `tar -tzf`. Незавершённый каталог удаляется,
а `latest` указывает только на полностью проверенную копию. Права каталога —
`700`, файлов — `600`.

Хранение: 14 дней ежедневных, 70 дней еженедельных и 90 дней ежемесячных
копий. Недельная копия создаётся по воскресеньям, месячная — первого числа;
они используют hard links и не дублируют неизменившиеся файлы на диске.

Проверка и ручной запуск:

```bash
systemctl list-timers incident-bot-backup.timer --all
systemctl status incident-bot-backup.timer
systemctl start incident-bot-backup.service
journalctl -u incident-bot-backup.service -n 100 --no-pager

cd /opt/incident-bot/backups/latest
sha256sum -c SHA256SUMS
cd /opt/incident-bot
docker compose exec -T postgres pg_restore --list \
  < backups/latest/incident.dump >/dev/null
```

Первый прогон `daily-20260902-112610` успешно проверен: 96 объектов в дампе,
архив вложений читается, приложение осталось `ready`, все 11 обращений на
месте. Локальная автоматизация не заменяет внешнюю копию: до production нужно
настроить отправку проверенного комплекта на отдельный сервер или S3.

---

## 5.1. Юридические документы и допуск к созданию обращения

Что делает бот:

1. Команда `/start` показывает главное меню; раздел «Документы» доступен всегда.
2. При первом нажатии «Создать обращение» бот показывает ссылки на три PDF.
3. Пользователь отдельно принимает Пользовательское соглашение и отдельно даёт
   согласие на обработку персональных данных. Одно нажатие не заменяет другое.
4. Только после обоих подтверждений открывается создание обращения.
5. В БД сохраняются MAX ID пользователя, тип подтверждения, редакция и SHA-256
   документа, точный текст кнопки, время, ID callback, сообщения и чата.
6. Повторное нажатие не создаёт дубль. Если поменять
   `LEGAL_DOCUMENT_VERSION`, прежние подтверждения перестанут давать доступ.

Защитное правило на будущее: пока в новой редакции есть незаполненные поля или
она не согласована, функция должна оставаться выключенной:
`LEGAL_CONSENT_REQUIRED=false`. Caddy публикует только отдельный каталог
`legal/public`, а не рабочие Word-файлы или промежуточные материалы.

Порядок финального включения:

```bash
cd /opt/incident-bot

# 1. Положить три одобренных юристом PDF под этими точными именами:
# legal/public/user-agreement.pdf
# legal/public/privacy-policy.pdf
# legal/public/personal-data-consent.pdf

# 2. Получить контрольные суммы для .env:
sha256sum legal/public/user-agreement.pdf
sha256sum legal/public/privacy-policy.pdf
sha256sum legal/public/personal-data-consent.pdf

# 3. В .env задать:
# LEGAL_DOCUMENTS_BASE_URL=https://<PUBLIC_DOMAIN>/documents
# LEGAL_DOCUMENT_VERSION=1.0
# LEGAL_USER_AGREEMENT_SHA256=<сумма user-agreement.pdf>
# LEGAL_PRIVACY_POLICY_SHA256=<сумма privacy-policy.pdf>
# LEGAL_PERSONAL_DATA_CONSENT_SHA256=<сумма personal-data-consent.pdf>
# LEGAL_CONSENT_REQUIRED=true

# 4. Перезапустить Caddy и приложение, затем проверить документы и БД:
docker compose --profile tls up -d --build app caddy
NODE_EXTRA_CA_CERTS=./deploy/russian-trusted-ca.pem npm run doctor
```

Проверить, сколько подтверждений сохранено по текущей редакции:

```bash
docker compose exec -T postgres psql -U incident -d incident -c '
SELECT "type", "documentVersion", count(*)
FROM "LegalAcceptance"
GROUP BY "type", "documentVersion"
ORDER BY "documentVersion", "type";'
```

При любом содержательном изменении PDF нужно увеличить
`LEGAL_DOCUMENT_VERSION`, пересчитать SHA-256 и снова выполнить `doctor`.
Подменять файл, сохраняя прежние версию и контрольную сумму, нельзя.

Механизм развёрнут 4 сентября 2026 после проверенного бэкапа
`daily-20260904-095336`. На отдельной PostgreSQL прошли 166/166 тестов.
Миграция `20260904123000_legal_acceptances` применена, приложение и Caddy
перезапущены, `/ready` отвечает. В рабочей базе сохранены 3 обращения и 50
ответственных групп, очереди доставки пусты. `doctor` показывает 59 успешных
проверок, 1 ожидаемое предупреждение о выключенном юридическом допуске и 0
ошибок. Каталог публикации пуст, запрос чернового документа снаружи возвращает
404; `LegalAcceptance` содержит 0 записей до будущего включения функции.

Финальные документы редакции 1.0 опубликованы и обязательный допуск включён
4 сентября 2026 после проверенного бэкапа `daily-20260904-111303`. Перед
публикацией исправлен ОГРН Оператора на `1194027000221`, удалены черновые
названия файлов и личные метаданные Word, визуально проверены все 6 страниц.
Публичные PDF и их SHA-256:

* `user-agreement.pdf` — `1856811ef9c175f4ec9e046970206900caa052e2e76ee8fef7c3334c4c7f0453`;
* `privacy-policy.pdf` — `8937eb286cf2b76a3c1c2898741e74bab03c2324158807e2b630b7289ceca4d8`;
* `personal-data-consent.pdf` — `12484438aacec0ad0c99aa0b2805d6981f980fadca1211ed10a9e4b10f325c0c`.

Флаг `LEGAL_CONSENT_REQUIRED=true` загружен в рабочий контейнер, `/ready`
отвечает, `doctor` — 62/62 без предупреждений и ошибок. На момент включения в
рабочей базе сохранены 4 обращения и 50 ответственных групп, очереди доставки
пусты.

---

## 6. Перед боевым запуском

Настройки, оставшиеся в тестовом виде:

- [ ] `DAILY_INCIDENT_LIMIT` вернуть с **50** на **2** (поднимали, чтобы лимит не мешал прогону)
- [x] `LOG_LEVEL` возвращён с `debug` на `info` 2026-09-02; важные события
      и ошибки сохраняются, отладочный шум отключён
- [x] Роль текущего администратора перенесена из `.env` в БД 2026-09-02;
      `ADMINS` очищен, диагностика подтверждает одного постоянного `ADMIN`
- [x] Добавлены постоянные очереди входящих и исходящих сообщений, контроль
      ошибок доставки и защита от повторных нажатий кнопок
- [x] Настроены автоматические предупреждения о проблемах доставки в отдельный
      технический чат MAX
- [ ] Раздать роли реальным сотрудникам: DISPATCHER на распределение, APPROVER на согласование
- [x] После обновления проверены все 50 профильных чатов командой
      `NODE_EXTRA_CA_CERTS=./deploy/russian-trusted-ca.pem npm run doctor`:
      4 сентября 2026 диагностика завершилась 59/59 без ошибок
- [x] Настроить ежедневный локальный бэкап БД, вложений и конфигурации с
      проверкой целостности и ротацией
- [ ] Перед публикацией юридических документов проверить очистку старше 90
      дней на production и разобрать ручные/внешние резервные копии
- [x] Заполнить реквизиты Оператора, контакт и дату во всех трёх документах,
      убрать черновые пометки и проверить PDF постранично
- [x] Опубликовать только финальные PDF, задать версию и SHA-256, включить
      `LEGAL_CONSENT_REQUIRED=true` и выполнить `doctor`: 62/62
- [ ] Пройти подтверждение обоих документов новым тестовым пользователем и
      проверить две сохранённые записи в `LegalAcceptance`
- [ ] Настроить автоматическую внешнюю копию бэкапов на другой сервер или S3
- [ ] При росте объёма вложений перевести `MEDIA_STORAGE` на `s3`

Безопасность:

> **SSH-hardening сознательно отложен до самого последнего действия перед
> production.** Сервер и бот ещё могут переехать; ключи, пользователь и
> ограничения SSH нужно настраивать уже на окончательной машине. Этот блок
> является блокирующим: production нельзя считать готовым, пока все пункты
> ниже не закрыты.

- [ ] Сразу после текущей рабочей сессии сменить временный пароль root — он
      передавался в переписке. Это делает владелец сервера, не откладывая до
      финального SSH-hardening
- [ ] На окончательном сервере создать отдельного администратора с `sudo` и
      новым выделенным Ed25519-ключом
- [ ] Проверить вход ключом и `sudo` во второй SSH-сессии, не закрывая
      действующую root-сессию
- [ ] Удалить или явно подтвердить необходимость старых ключей
      `metricflow-codex-deploy` и `audience-base-codex-deploy`
- [ ] После `sshd -t` отключить парольный вход и прямой root-вход
      (`PasswordAuthentication no`, `PermitRootLogin no`), затем выполнить
      `reload` и повторно проверить доступ
- [ ] Включить Fail2ban для SSH и проверить, что доверенный адрес не блокируется
- [ ] Перевыпустить `BOT_TOKEN` у `@MasterBot` — передавался в переписке
- [ ] Отозвать ключ AITunnel — передавался в переписке; ИИ из проекта удалён,
      ключ больше нигде не используется, но действующим оставлять его нельзя
- [ ] `WEBHOOK_SECRET` сгенерирован на сервере и в переписке не светился — менять не обязательно

Не проверено вживую (закрыто только тестами):

- [ ] **Сценарий 7** — два пользователя, два ответа. Нужен второй аккаунт MAX. Тестами закрыт дважды (юнит на фейках + интеграционный на реальной БД), но это единственная проверка про то, чтобы ответ не ушёл чужому человеку

---

## 7. Что решили и почему

Короткие обоснования, чтобы не переоткрывать вопросы заново.

**26 тем выбираются страницами по 6.** Один список на 27 кнопок — длинная
прокрутка на телефоне. «Не знаю» закреплено сверху на каждой странице: выбор
пользователя по §8 ТЗ всё равно только предположение, маршрут определяет
диспетчер.

**ИИ убран из проекта целиком (сентябрь 2026).** Классификация, AI-черновик и
AI-модерация работали и были измерены (90–98% попадания по сфере, 1.4–2.2 с,
~2 копейки за вызов), но каждый вызов означал отправку текста обращения жителя
стороннему обработчику. Обращение — это персональные данные, и без отдельного
правового основания такая передача недопустима, поэтому выключением флага
ограничиваться не стали: удалён код (`src/ai/`), зависимость на SDK моделей,
переменные `AI_*` и столбцы `ai*` в БД. Возвращать имеет смысл только с моделью
в контуре заказчика либо с согласием заявителя и договором с обработчиком;
предыдущая реализация лежит в git, в коммите до удаления.

**Отчёты — кнопками.** `/report` без аргументов показывает периоды; ручной ввод
принимает `01.08.2026 - 10.08.2026` в любом написании. Прежний синтаксис
(`/report 7d`, ISO-даты) сохранён, чтобы не ломать скрипты и привычки.

**Права проверяются при нажатии, а не при показе кнопок.** Карточка остаётся в
истории чата, нажать её может кто угодно позже — поэтому факт существования
кнопки ничего не разрешает.

**Цветные статусы только в чате «Распределение».** `🔴 НЕ
РАСПРЕДЕЛЕНО` означает, что обращение ещё ждёт выбора группы;
`🟡 РАСПРЕДЕЛЕНО` — группа выбрана; `🟢 ОТРАБОТАНО` — ответ
доставлен пользователю. После выбора группы временное меню удаляется,
а исходная карточка меняет статус. В профильных чатах эти метки не
показываются.
Механизм развёрнут 4 сентября 2026 после проверенного бэкапа
`daily-20260904-113722`. На отдельной тестовой БД прошли 168/168
тестов; `/ready` отвечает, `doctor` — 62/62 без предупреждений и ошибок.
Задвоение и область показа меток исправлены в тот же день после бэкапа
`daily-20260904-115544`: 173/173 теста, `/ready` отвечает, `doctor` — 62/62.
После подготовки ответа в профильный чат отправляется ровно одно
итоговое уведомление: его формирует обработчик диалога с указанием версии.
Исправление развёрнуто после бэкапа `daily-20260904-120543`: 173/173 теста,
`/ready` отвечает, `doctor` — 62/62.

**ФИО и телефон обязательны для каждого нового обращения.** После принятия
документов бот последовательно запрашивает фамилию и имя (отчество — при
наличии), затем номер телефона. Некорректное значение не переводит пользователя
дальше. Телефон приводится к единому виду `+7 900 123-45-67`; оба значения
показываются в рабочих карточках и попадают в отдельные столбцы отчёта. Старые
обращения сохранены без изменения и могут не иметь телефона. Механизм развёрнут
4 сентября 2026 после бэкапа `daily-20260904-122349`, миграция
`20260904160000_requester_contacts`; на отдельной тестовой БД прошли 177/177
тестов, `/ready` отвечает, `doctor` — 62/62 без предупреждений и ошибок.

**Перед отправкой пользователь подтверждает итоговую карточку.** До нажатия
«Всё верно» строка обращения в БД не создаётся, дневной лимит не расходуется и
в рабочие чаты ничего не отправляется. Кнопка «Исправить» меняет только
выбранное поле; тему и территорию пользователь выбирает прежними листаемыми
кнопками, фотографии можно заменить или удалить. Быстрые повторные нажатия
«Всё верно» защищены длительной блокировкой действия, а после успешной
регистрации кнопки карточки убираются. Механизм развёрнут 4 сентября 2026 после
бэкапа `daily-20260904-125629`, миграция
`20260904173000_incident_confirmation`; на отдельной тестовой БД прошли 181/181
тестов, `/ready` отвечает, `doctor` — 62/62 без предупреждений и ошибок.

**Трёхдневный SLA является только внутренним.** Он продолжает рассчитываться
в БД и показываться сотрудникам в рабочих карточках, истории, предупреждениях
и отчётах, но обычному пользователю не сообщается. Из приветствия, правил,
подтверждения регистрации и раздела «Мои обращения» убраны срок ответа и
отметка просрочки. Приветствие и правила переписаны под полный текущий путь:
документы, обязательные ФИО и телефон, сфера, территория, описание,
фотографии, проверка и исправление итоговой карточки. Изменение развёрнуто
4 сентября 2026 после бэкапа `daily-20260904-201026`; на отдельной тестовой БД
прошли 185/185 тестов, `/ready` отвечает, `doctor` — 62/62 без предупреждений
и ошибок.

**Заявитель оценивает итоговый ответ по шкале от 1 до 5.** Кнопки находятся
под доставленным ответом. База принимает только первую оценку, проверяет автора
обращения и факт доставки; параллельные и повторные нажатия значение не
перезаписывают. Оценка показывается в `/history` и в отдельном столбце
Excel-отчёта, а её отсутствие остаётся пустой ячейкой. Механизм развёрнут
4 сентября 2026 после проверенного бэкапа `daily-20260904-203726`, миграция
`20260904210000_answer_rating`; на отдельной тестовой БД прошли 188/188 тестов,
`/ready` отвечает, `doctor` — 62/62 без предупреждений и ошибок.

**ФИО и телефон переиспользуются из профиля заявителя.** При первом обращении
номер можно ввести текстом или передать штатной кнопкой MAX «Поделиться
контактом»; пересланный контакт другого пользователя не принимается. Профиль
записывается только вместе с подтверждённым обращением. При следующем обращении
контакты сразу попадают в черновик, а исправление в итоговой карточке обновляет
профиль. Миграция переносит данные существующих пользователей из их последнего
обращения. Политика удаления не изменилась: неактивный профиль очищается по
правилу 90 дней. Механизм развёрнут 5 сентября 2026 после проверенного бэкапа
`daily-20260904-210636`, миграция `20260904223000_requester_profile`; на
отдельной тестовой БД прошёл 191/191 тест, `/ready` отвечает, `doctor` — 62/62
без предупреждений и ошибок.

**Ответственная группа без чата скрыта от диспетчера, но это не ошибка в доктора.** Это
состояние внедрения; если считать его ошибкой, доктор будет вечно красным и
перестанет что-либо значить.

**5 сентября 2026 — пункт 1: транзакционная постановка отправок.** Регистрация,
распределение, отклонение, подготовка и согласование ответа, возврат на
доработку и предупреждения SLA сохраняют состояние, историю и исходящее
задание одной транзакцией. Подтверждённый черновик закрывается атомарно.
Очередь использует оригиналы вложений по ссылкам, не удаляя их после отправки.
Изменения: `da5d4a0`, `35d097f`; миграция схемы не требовалась.
Локально прошли typecheck, build и 144 unit-теста. На отдельной базе
`incident_test` — полный набор 202/202, включая 11 новых проверок транзакций,
сбоев очереди и восстановления отправки новым worker. Перед пересборкой app
создан бэкап `daily-20260905-194343`: SHA256SUMS проверены, `pg_restore --list`
успешен. После развёртывания `/ready` отвечает, все три контейнера работают,
PostgreSQL healthy, doctor 62/62. Очереди пусты, в проверенных последних логах
нового app нет предупреждений и ошибок. Остальные пункты аудита выполняются
отдельно; данный выпуск не меняет правила показа статуса доставки и доступа.

**5 сентября 2026 — пункт 2: фактическая доставка ответа.** Коммит `39c6705`
разделяет «ожидает доставки», «доставлен» и «доставлен ранее» для согласования,
прямого регионального ответа и /resend. Зелёная карточка появляется только
после доставки. Успешный повтор сохраняет задания обновления рабочих карточек
и одно уведомление профильной группе в исходящей очереди. Карточки при
выполнении задания читают актуальный deliveredAt. Локально: typecheck, build,
145 unit-тестов; серверная incident_test: 205/205. Бэкап перед пересборкой:
`daily-20260905-195558`, SHA256SUMS и pg_restore --list успешны. После
развёртывания ready отвечает, контейнеры работают, doctor 62/62, очереди пусты;
в проверенных логах нового app нет предупреждений и ошибок.

**5 сентября 2026 — пункт 3: доступ только из настроенных рабочих чатов.**
Коммит `1b4cc78` ограничивает команды сотрудников, кнопки отчётов и обработку
операторских сессий центральными чатами и активными ответственными группами.
Проверка действует и для ADMIN; /chatid остаётся доступной для настройки.
Локально typecheck, build и 147 unit-тестов; incident_test — 207/207.
Бэкап `daily-20260905-200744` проверен по SHA256SUMS и pg_restore --list.
После пересборки app ready отвечает, doctor 62/62, очереди пусты,
в последних логах приложения нет предупреждений и ошибок.

**5 сентября 2026 — пункт 4: согласование конкретной версии ответа.** Коммит
`f9da8e0`: кнопки и сессия возврата содержат answerId, смена версии проверяется
повторно под блокировкой строки обращения. Очередь обновляет reviewMessageId
для новой версии и не принимает запоздалую карточку предыдущей. Старые кнопки
допускаются только при совпадении записи исходящей доставки с последней версией.
Локально typecheck/build и 147 unit-тестов; incident_test — 210/210.
Бэкап `daily-20260905-201425`: SHA256SUMS и pg_restore --list успешны.
После развёртывания ready отвечает, doctor 62/62, очереди пусты, последние
логи приложения без предупреждений и ошибок.

**5 сентября 2026 — пункт 5: технические ошибки видны во входящей очереди.**
Коммит `176b931` передаёт неожиданные исключения из команд, кнопок и диалогов
в dispatcher: FAILED вместо PROCESSED. Ошибки ввода и отказы по правам остаются
штатными; сообщение пользователю сохраняется. Сбой показа сообщения не маскирует
первопричину. Локально typecheck/build, 149 unit-тестов; incident_test — 220/220,
включая восемь сценариев классификации ошибок через настоящий inbox.
Бэкап `daily-20260905-202058` проверен по SHA256SUMS и pg_restore --list.
После установки ready отвечает, doctor 62/62, очереди пусты, последние логи
приложения без предупреждений и ошибок.

**5 сентября 2026 — пункт 6: вложения не пропускаются молча.** Коммит `6d2275c`
требует сохранения каждого поддерживаемого вложения и чтения всех файлов при
отправке. Ошибка сохраняет черновик/статус и исходящее задание для повторной
попытки; неподдерживаемые вложения явно отклоняются. Загрузка из MAX ограничена
30 секундами на серию сетевых попыток. Новые файлы очищаются при отказе внутри
транзакции после проверки отсутствия владельца; при неоднозначном COMMIT файлы
сохраняются. Локально typecheck/build, 154 unit-теста; incident_test — 228/228.
Бэкап `daily-20260905-202921` проверен по SHA256SUMS и pg_restore --list.
После установки ready отвечает, doctor 62/62, очереди пусты; последние логи
приложения без предупреждений и ошибок.

**5 сентября 2026 — пункт 7: корректная остановка перед перезапуском.** Коммит
`78b15e6` прекращает приём/выбор новых входящих заданий, ждёт текущие обработчики,
фоновые проверки и отправки, затем отключает PostgreSQL. Новые исходящие задания
во время завершения сохраняются для следующего запуска. Ожидание ограничено
60 секундами; при превышении процесс завершается с ошибкой, не отключая БД под
работающим обработчиком. Docker получает 75 секунд, Node запускается через exec.
Локально typecheck/build, 157 unit-тестов; incident_test — 233/233. Проверены
активная входящая операция, активная отправка, возобновление очереди новым
worker, поздний ответ polling и предельное время завершения.
Бэкап `daily-20260905-203821` проверен по SHA256SUMS и pg_restore --list.
После установки выполнен реальный docker compose restart app: журнал подтвердил
`graceful shutdown completed`. После повторного запуска ready отвечает,
doctor 62/62, очереди пусты; 14 проверенных записей приложения без предупреждений
и ошибок. Все семь пунктов аудита выполнены отдельными проверенными выпусками.

**5 сентября 2026 — одно подтверждение регистрации.** Коммит `dc1ecbf` удаляет
карточку проверки черновика после успешной регистрации вместо её превращения
в дополнительное сообщение «Данные подтверждены / Обращение зарегистрировано».
Пользователю остаётся полное подтверждение с номером, датой и главным меню;
его транзакционная очередь и защита от повторной отправки сохранены.
Локально typecheck/build и 157 unit-тестов; incident_test — 233/233.
Перед установкой проверен бэкап `daily-20260905-204854` (SHA256SUMS,
pg_restore --list). После обновления ready отвечает, doctor 62/62, очереди
пусты, последние логи приложения без предупреждений и ошибок.

**6 сентября 2026 — второй лист Excel с текущими просроченными.** Коммит
`d0fbd95` добавляет лист «Просроченные»: все открытые обращения с истекшим
на момент формирования deadlineAt независимо от периода первого листа.
RESOLVED и REJECTED исключаются; согласование и доработка включаются.
Отбор не зависит от сохранённого isOverdue. Указаны время формирования,
дедлайн, просрочка в часах, статус, ответственные, описание и данные заявителя;
сортировка от самой давней просрочки. При отсутствии записей лист содержит
понятную отметку. Если первый лист пуст, но просроченные есть, файл отправляется.
Локально typecheck/build и 157 unit-тестов; incident_test — 236/236, включая
три проверки нового отчёта с чтением итогового XLSX и границей ровно 72 часа.
Бэкап `daily-20260905-210557` проверен по SHA256SUMS и pg_restore --list.
После установки ready отвечает, doctor 62/62, очереди пусты. Дополнительное
формирование и чтение XLSX внутри app подтвердило порядок двух листов и колонку
просрочки; сообщения в MAX для этой проверки не отправлялись.


**6 сентября 2026 — напоминания через сутки и двое суток с переходом к карточке.**
Коммит `c018c74` (подготовка тестовой карточки уточнена в `70d74d0`) заменяет
предупреждения за 24/6 часов на ступени через 24/48 часов после регистрации и
уведомление при deadlineAt (72 часа). Проверка на сервере включена, интервал 10 минут.
Напоминание отправляется в назначенный профильный чат ответом на исходную карточку;
до распределения — ответом на карточку распределения. Указаны группа, сотрудник,
статус, регистрация и дедлайн. Согласование и доработка включаются, закрытые исключаются.
После простоя выбирается только актуальная ступень, повторные проверки не дублируют её.
Миграция добавляет nullable slaReminder24SentAt; старая метка 24 часов до дедлайна
сохраняется для ступени 48 часов, чтобы не повторять существующие напоминания.
История и outbox сохраняются вместе с меткой. Очередь перед доставкой проверяет
актуальность, перечитывает ответственных и карточку; при задержке карточки ждёт
через штатные повторы, при закрытии или следующей ступени пропускает устаревшую.
Локально typecheck/build и 158 unit-тестов; полный повторный прогон incident_test —
247/247, 38 файлов. Проверены границы сроков, конкуренция, legacy-метка, согласование,
доработка, закрытие, ответ в оба вида чатов, сбой MAX и возобновление новым worker.
Бэкап `daily-20260905-212713` проверен по SHA256SUMS и pg_restore --list.
После пересборки app миграция применена, ready отвечает, doctor 62/62, очереди пусты,
логи без предупреждений и ошибок. У всех трёх открытых обращений есть карточки.
Плановый контроль автоматически отправил актуальное напоминание; чтение его через
MAX API подтвердило type=reply, ссылку на правильную карточку и правильный чат.
Ручные тестовые сообщения в рабочие чаты не отправлялись.


**6 сентября 2026 — конфигурация Vitest в ESM.** Коммит `2a4771a`
переименовывает vitest.config.ts в vitest.config.mts без изменения настроек.
Vitest загружает конфигурацию как ESM; предупреждение о deprecated CJS Node API
Vite больше не выводится ни локально, ни на сервере. Локально typecheck и
158 unit-тестов; полный прогон в incident_test — 247/247, 38 файлов.
На сервере заменён только конфиг тестов; прежний файл сохранён в
/tmp/iskra-vitest.config.before-esm.ts. Работающий контейнер не перезапускался.


**6 сентября 2026 — предложение подписаться после решения обращения.**
Коммит `a64ca76` ставит отдельное сообщение с приглашением подписаться в очередь
только после успешной доставки утверждённого ответа по RESOLVED-обращению.
Текст: «Ответы на волнующие вас вопросы можно также узнать в этих каналах.
Подпишитесь:». Две кнопки ведут на https://max.ru/Shapsha_VV и
https://max.ru/pravitelstvo40. Кнопки оценки остаются на итоговом ответе.
Предложение создаётся одной транзакцией с отметкой доставки ответа, ключ
subscription-invite:<incidentId> защищает от повторов. Сбой предложения
повторяет только предложение; до доставки ответа и после отклонения оно не создаётся.
Локально typecheck/build и 158 unit-тестов; incident_test — 248/248, 38 файлов.
Проверены обе схемы ответа, порядок, получатель, точные URL кнопок, отсутствие
предложения при недоставленном ответе, восстановление предложения новым worker
без повторной отправки ответа и отсутствие предложения после отклонения.
Бэкап `daily-20260906-110108` проверен по SHA256SUMS и pg_restore --list.
После установки ready отвечает, doctor 62/62, очереди пусты, последние логи
без предупреждений и ошибок. Ручные тестовые сообщения жителям не отправлялись.


**6 сентября 2026 — подписка только после оценки ответа.** Коммит `a5ae0be`
переносит создание приглашения из подтверждения доставки в транзакцию сохранения
оценки. Порядок: итоговый ответ с кнопками 1–5, выбор оценки, приглашение подписаться.
Без оценки приглашение не создаётся. Сохраняются обе ссылки, подтверждение оценки
и защита от повторов на обращение. При сбое очереди оценка и история откатываются
вместе, чтобы нажатие можно было повторить. Старые ожидающие приглашения worker
удерживает до оценки без расхода попыток; сохранение оценки освобождает их.
Уже отправленные приглашения повторно не создаются.
Локально typecheck/build и 158 unit-тестов; полный прогон incident_test — 250/250,
38 файлов. Проверены обе схемы ответа, отсутствие приглашения до оценки,
повторная доставка приглашения, одновременные оценки, атомарность и старая очередь.
Бэкап `daily-20260906-112334` проверен по SHA256SUMS и pg_restore --list.
После установки ready отвечает, doctor 62/62, очереди пусты, логи без предупреждений
и ошибок. Ручные тестовые сообщения жителям не отправлялись.


**6 сентября 2026 — уточнения у жителя с приостановкой срока.** Коммит `310033a`
добавляет в профильную карточку кнопку «💬 Уточнить у жителя». Автор вопроса
проверяет текст и подтверждает отправку. Житель отвечает кнопкой в личном чате:
текст до 3000 символов и/или до четырёх фотографий одним сообщением. Ответ
отправляется цитатой исходной секторной карточки. Одновременно ожидается один
вопрос; итоговый ответ заблокирован до уточнения. Вопрос, ответ, фото и события
сохраняются с обращением, удаляются по общим правилам хранения.
Срок останавливается после подтверждённой MAX доставки вопроса. После сохранения
ответа deadlineAt увеличивается на время ожидания, slaPausedMs накапливает паузы.
Напоминания и лист просрочек исключают паузу; первый лист показывает ожидание
уточнения. Отложенные напоминания удерживаются без расходования попыток.
Смена состояния, история и outbox атомарны. Конкурирующие запросы и ответы
защищены общей с отправкой итогового ответа блокировкой; повтор не продлевает срок.

Проверки: локально typecheck/build и 158 unit-тестов; полный прогон incident_test —
256/256, 39 файлов. После добавления проверки удержания уже поставленного в очередь
напоминания повторно пройдены typecheck и все 7 тестов clarifications.test.ts.
Проверены кнопки и обработчики с текстом/фото, автор и чат, правильный получатель,
цитирование нужной карточки, конкуренция, откат при сбоях очереди и медиа, доставка
после сбоя MAX, две паузы и сохранение оставшегося времени (20 ч + 100 ч ожидания
оставляют 52 ч), отсутствие оценки/подписки, удаление файлов уточнения при retention.
Полная цепочка из 17 миграций применена в отдельной временной схеме incident_test;
модели проверены, временная схема удалена. Рабочая БД не использовалась для тестов.

Бэкап `daily-20260906-115318` проверен по SHA256SUMS и pg_restore --list.
На сервер переданы только изменённые файлы, app пересобран и запущен;
миграция `20260906120000_clarifications` применена. Ready отвечает, doctor 62/62,
входящие/исходящие очереди пусты, стартовые логи без предупреждений и ошибок.
В 3 существующих открытых карточках добавлена кнопка через штатную очередь
редактирования. Все 3 задания завершены, наличие кнопок проверено чтением MAX API.
В рабочей базе 13 обращений; ручные тестовые обращения/сообщения не создавались.


**6 сентября 2026 — постоянная очередь распределения.** Коммит `f49d0ca`
добавляет панель в чате распределения, `/queue`, список по 10 обращений и кнопки
«Следующее обращение», «Посмотреть список», «Обновить». Источник — БД, порядок
по созданию/ID; старое сообщение в истории чата не скрывает обращение из очереди.
Один оператор получает одно обращение на 15 минут; повтор возвращает его же.
Есть освобождение раньше срока. Другой оператор может забрать просроченное
закрепление, прежний владелец уже не может распределить/отклонить/освободить его.
Выдача и карточка сохраняются атомарно; распределение/отклонение проверяют владельца
внутри общей транзакционной блокировки. Терминальные действия очищают закрепление.
Общий SLA не останавливается и дедлайн не меняется.

Панель переиспользует одно задание outbox и сохранённый MAX message ID, обновляется
раз в минуту, восстанавливается после подтверждённого удаления сообщения (404).
Автозакрепление не заменяет другое закреплённое сообщение. Плановая сверка создаёт
отсутствующие задания карточек; существующие FAILED обслуживает обычный механизм
ошибок доставки. Очередь доступна только ADMIN/DISPATCHER в чате распределения.
Сводки настроены по прямому указанию пользователя: ежедневно 08:00–22:00 Москва.
Через час ожидания — сводка диспетчерам, через два — сигнал в DELIVERY_ALERT_CHAT_ID
(чат «Проблемы»). Порог перегрузки — 30 обращений. Не более одной сводки на чат за
календарный час; перед отправкой проверяются график, текущий час и состояние очереди.
Ночью приём и ручной разбор работают; существующий общий SLA работает отдельно.

Локальные typecheck/build и 160 unit-тестов прошли. Полный прогон incident_test:
266/266, 41 файл. Проверены конкуренция операторов, порядок, повторные клики,
истечение 15 минут, старые действия, освобождение, запрет чужого чата/роли,
атомарный откат при сбое очереди, сохранность дедлайна, одна панель после перезапуска,
обе границы рабочего окна и выходные, отложенная доставка ночью/после обработки,
сводки и эскалация без дублей, восстановление заданий карточек, очередь 30 записей
и получение всех страниц без потери хвоста. В отдельной временной схеме incident_test
проверена полная цепочка 18 миграций, после проверки схема удалена.

Бэкап `daily-20260906-122907` проверен SHA256SUMS и pg_restore --list.
На сервер переданы только изменённые файлы. В .env явно сохранены включение очереди,
08:00, 22:00 и порог 30. App пересобран; миграция
`20260906160000_distribution_queue` применена. Ready отвечает, doctor 62/62,
стартовые логи без предупреждений и ошибок. Чтение MAX API подтвердило все три
кнопки и закрепление панели; в БД ровно одно задание панели, FAILED нет.
На момент проверки 14 обращений, нераспределённых 0. Тестовые обращения и ручные
тестовые сообщения в рабочие чаты не создавались; опубликована рабочая панель очереди.

**6 сентября 2026 — исправления аудита, коммит `9c920ea`.** MAX callback всегда
содержит непустое уведомление («Принято.» по умолчанию). Скачивание вложений
ограничено 20 МБ на файл: `MEDIA_MAX_FILE_MB`, допустимый диапазон 1–100. Размер
проверяется по метаданным, HTTP-заголовку и фактически прочитанным байтам.
При превышении поток прерывается без повторного скачивания, пользователь получает
понятный отказ, предыдущие вложения незавершённой отправки очищаются. Копии файлов
по-прежнему сохраняются для надёжной доставки; каждый повтор после сетевого сбоя
получает новый тайм-аут 30 с. В текущем .env лимит не переопределён: действует 20 МБ.

Для `@prisma/config` закреплён `deepmerge-ts 8.0.0`, для ExcelJS — `uuid 11.1.1`.
Prisma 6.19.3 и ExcelJS 4.4.0 сохранены. Overrides нужны, пока родительские пакеты
не включили исправленные версии; снимать их следует только после проверки audit
и совместимости. Локальные зависимости синхронизированы с серверным lock-файлом.
Typecheck/build прошли локально и на сервере; полный прогон incident_test —
280/280 тестов, 43 файла, без пропусков. Тесты БД запускаются последовательно,
реквизиты берутся из окружения с обязательной заменой имени базы на incident_test;
пример пароля incident:incident в старых разделах не соответствует текущему серверу.

Бэкап `daily-20260906-153109` проверен по SHA-256 и pg_restore --list.
Код: `backups/pre-audit-fixes-20260906-1533/code.tar.gz`, прежний образ:
`incident-bot-app:pre-audit-fixes-20260906-1533`. Пересобран и перезапущен только app.
Ready отвечает, doctor 62/62; очередь исходящих 302 SENT, входящих 210 PROCESSED,
ожидающих/ошибочных нет. В базе 16 обращений. Host/container dist SHA-256:
`81045abb51384d6999b9b9841d439410445c2573bd52e5444e4cc8505109153a`.
Рабочий контейнер содержит проверенные версии, лимит 20 МБ и не содержит Vitest;
`npm audit --omit=dev` внутри него — 0 уязвимостей. При старте остаётся прежнее
предупреждение CLI о package.json#prisma; ошибок приложения не обнаружено.
Тестовые сообщения жителям и в рабочие чаты не отправлялись.

Отдельно: полный npm audit включает 5 предупреждений цепочки инструментов
разработки Vitest/Vite/esbuild (3 moderate, 1 high, 1 critical). Рабочий образ
собирается с `--omit=dev`, тесты запускаются без UI/dev-сервера. Обновление
тестовой цепочки требует отдельной проверки совместимости. Подробности —
в `AUDIT-2026-09-06.md`.
